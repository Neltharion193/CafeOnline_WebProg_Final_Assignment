steps:
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
#specify the name of the image
  args: ['build', '-t', 'gcr.io/$noted-flux-391501/cafeonline_webprog_final_assignment-main', '.']
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$noted-flux-391501/cafeonline_webprog_final_assignment-main']
# Deploy container image to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
#specify the service name and the region
  args: ['run', 'deploy', 'cafeonline-web', '--image', 'gcr.io/$noted-flux-391501/cafeonline_webprog_final_assignment-main', '--region', 'asia-southeast2',         
        "--platform", "managed",
        "--port", "8080"]
images:
- gcr.io/$noted-flux-391501/cafeonline_webprog_final_assignment-main